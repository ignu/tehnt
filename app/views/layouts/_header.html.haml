%a{:href=>'/'}
  .logo=image_tag ("logo_small.jpg")
-if admin?
  .nav{:style=>'padding-left:90px'}
    =link_to 'home', "/"
    =link_to 'logout', "/logout" if admin?
    =link_to 'reports', "/reports" if admin?
    = yield :admin_nav